---
description: ולידציה מחמירה לפני deploy — מפעיל את outcomes-grader (שמאחד גם prod-gatekeeper) דרך Task tool
argument-hint: [dev | prod]
---

הפעל את **Task tool** עם `subagent_type: "outcomes-grader"` כדי לבצע ולידציה מלאה לפי הפרוטוקול ב-`.claude/agents/outcomes-grader.md`.

ה-outcomes-grader כולל עכשיו את שכבת ה-PROD Safety (Zero-context analysis + 3 crash scenarios + pre-write backup + idempotency + per-record error handling + rollback path) שהייתה פעם בסוכן הנפרד `prod-gatekeeper`.

**סביבת יעד:** {{ARGS}}

**הנחיות חובה לסוכן:**
- הפעל את פרוטוקול הספקנות שלך לפני כל טענה (ציטוט file:line, אימות קיום הקוד ב-grep/glob, תקרת 3 Reads, אסור "מצאתי" כוזב).
- אכוף את חוקי CLAUDE.md: main=DEV, production-stable=PROD, cache-bust חובה, smoke test חובה, אסור לעקוף branch protection.
- אם target_env=PROD — הפעל את שכבת PROD Safety בנוסף ל-Code Review.
- כל בדיקה חייבת ראיה בפועל (URL נבדק, קובץ log נקרא, פקודה רצה) — לא ספקולציה.
- VERDICT: `PASS` / `PASS_WITH_WARNINGS` / `FAIL` בלבד. אם חסר מידע — החזר "אין לי ודאות" ועצור.
