# Rubric — PR-T2-2: management card adopts the shared identity band (reading-pane + calm hours)

**Track:** Track-2 (reading-pane for the management card), PR-2 of 4 — the HIGH-STAKES one. The management service card (`UnifiedServiceCard.buildManageDetail`) adopts the SHARED `buildIdentityBand` (from PR-1) in place of its old `.management-service-header`, plus a calm status pill; and the unclamped "5838%" `.management-hours-progress` bar is removed (hours-utilization now rides the band's full-width 4px meter — the width + status placement Haim approved by seeing the real render).
**Scope:** frontend-only, DEV, admin-critical. 4 files: `UnifiedServiceCard.js` (header→band, status pill, remove 5838% + dead helpers), `clients-modals.css` (`.usc-identity-status` pill, tokens only), `clients.html` (`?v=` bump), `manage-detail-equality.test.ts` (re-anchor to the live contract). `buildOverride`/packages/footer/injectors untouched (buildOverride yellow-box + packages `<details>` + `⋯עוד` footer are LATER PRs).
**Type:** admin-critical display restructure. **devils-advocate MANDATORY** (§3.8.4 — admin-critical + the injector-contract + a declared behavioral display change).

## MUST (all required for PASS)

- **M1 — header → shared band.** `buildManageDetail` replaces the entire `.management-service-header` block with `${buildIdentityBand(card, { statusHtml: statusPillHtml(card.status) })}`. The card root `.management-service-card[data-service-id]` + `.management-service-body`→`.management-service-content`→`.management-service-info` + the legal `buildStagesHtml` + `.management-service-actions`→`buildActions` are ALL preserved.
- **M2 — report tab byte-identical.** `buildIdentityBand` gained an optional `opts` param; the status slot is `((opts && opts.statusHtml) ? opts.statusHtml : '')`. The report tab calls `buildIdentityBand(card)` (no opts) → output unchanged. **Proven:** `report-tab.test.ts` 38 green.
- **M3 — "5838%" removed, no info lost.** The `.management-hours-progress` block (unclamped `${percentage}%`) + its now-unused locals are deleted from the hours branch; utilization now = the band's CLAMPED meter, and used/total/remaining are preserved in the band stat (`40.0 / 50.0 ש׳ · נותרו 10.0`). No LIVE `.management-hours-progress` remains (grep: 0 live; 1 is a doc comment).
- **M4 — injector contract intact.** grep-verified on `buildManageDetail` output: `.management-service-card` root + `[data-service-id]`; all 5 `data-service-action` (renew/next-stage/change-status/complete/delete); `.management-stage`/`.management-stage-name`(===stage.name)/`.management-stage-info` (buildStagesHtml untouched); `.override-btn`/`.edit-pkg-date-btn`+data-* (buildOverride/buildPackagesBreakdown untouched). ReportTab.js emits 0 `.management-*`.
- **M5 — status pill + dead-code removal.** `statusPillHtml(status)` → `.usc-identity-status.usc-identity-status--<status>` (active/completed/on_hold/archived); CSS tokenized (no hardcoded hex). The now-dead `MANAGE_TYPE_BADGE`/`MANAGE_STATUS_BADGE`/`manageServiceIcon`/`manageStatusClass` are removed (eslint 0 unused). `truncate` kept (rail).
- **M6 — test re-anchored (false-green fix).** `manage-detail-equality.test.ts` no longer asserts the DEAD renderer's header equality (which PR-2 intentionally breaks); it asserts the LIVE `buildManageDetail` contract — root+`data-service-id`, the 5 `data-service-action`, legal `.management-stage-name`===stage.name — plus proof the header + `.management-hours-progress` are gone and the band + pill replaced them. No injector-critical check weakened.
- **M7 — gates.** `node --check` OK; eslint 0 errors; stylelint 0; `?v=` bumped (`UnifiedServiceCard.js` + `clients-modals.css` → `20260805-t2-2-manage-band`, ReportTab.js unchanged); 82 tests green (report-tab 38 + manage-detail-equality 6 + management-contracts 25 + preview-contract 13).

## PRODUCT-GRADE GATES (expected)

- **G1** PASS — the removed `.management-hours-progress` printed an UNCLAMPED runaway percentage ("5838%") — a G1 display defect now GONE; the band's numbers are `toFixed(1)`'d + clamped. No undefined/NaN path.
- **G2** PASS — Rollback = single `git revert` (restores the old header + hours-progress + `?v=`). Code-only.
- **G3** N/A — read-only display; no write/update/delete path.
- **G4** PASS — report-tab 38 (byte-identity) + manage-detail-equality (re-anchored live contract) + management-contracts 25. Manual DEV smoke after merge: open a client's management card → each service shows the reading-pane band (icon+name+type badge+status pill+meter), the old "5838%"/verbose stats gone, actions + stages + packages intact.
- **G5** PASS — Hebrew unchanged/added (פעיל/הושלם/בהמתנה/בארכיון + the band's הליך משפטי/מחיר קבוע/שעות/נותרו/חריגה).
- **G6** — **BEHAVIORAL/DISPLAY CHANGE (declared, ADMIN SAFETY).** The management card header is restructured (band replaces the old header; status badge → calm pill; the verbose hours-progress + "5838%" removed, utilization via the band meter). **No count/filter/aggregate/injector/data change** — display-only; used/total/remaining preserved. This is a deliberate visual change to an admin-trusted screen, called out per apps/admin-panel/CLAUDE.md.
- **G7** N/A — no auth/PII/permissions surface.
