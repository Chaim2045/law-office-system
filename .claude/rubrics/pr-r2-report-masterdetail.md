# Rubric — PR-R2: report tab → master-detail (rail + detail), matching the approved mockup

**Track:** Report-tab master-detail rebuild — the VISIBLE payoff after PR-R1 parameterized `buildRailRow`. The live "הפקת דוח" tab was a flat form; the approved mockup (artifact 227d1403, source `<scratchpad>/modal-masterdetail.html`) shows it as a rail + detail like the manage tab. Haim-approved: rebuild report-first, wire the REAL "unassigned hours" number now.
**Scope:** **frontend-only.** Rewrite `ReportTab.js` (form → master-detail); `ClientManagementModal._switchTab` passes `this.dataManager` to `ReportTab.render`; additive `.report-*` CSS in `clients-modals.css`; `?v=` bump on 3 assets; retarget `report-tab.test.ts` DOM-shape assertions (keep the contract pins). `buildManageDetail` / the manage tab / rules / callables / `ReportGenerator` / `ReportPreview` — **UNTOUCHED**. `getFormData()` byte-preserved.
**Type:** admin-critical UI rewrite (>100 lines → devils-advocate MANDATORY §3.8.4). The report OUTPUT is unchanged (same `getFormData` → same engine); the SELECTION interaction changes + a NEW read-only "unassigned hours" note is added.

## MUST (all required for PASS)

- **M1 — `getFormData()` byte-identical (THE report-engine contract).** The method body is unchanged; it returns the same 9 keys `{clientId, clientName, startDate, endDate, service, serviceId, stage, reportType, reportFormat}`. A rail selection sets `service`/`serviceId`; a legal stage pick sets `stage`=stage.id and `service`=**`getStageName(stage.id)`** — byte-matching the OLD `buildReportSelectCards` payload so the generated report reads identically. Hours/fixed → `stage:''`.
- **M2 — master-detail structure (mockup parity).** `#cmReportPanel` renders a `.cm-split`: a rail (`role="radiogroup"`, one `buildRailRow(card,{role:'radio',ariaControls:'mgmtReportServiceDetail'})` per service) + a detail (date bar at top with the 5 quick-date buttons + `#mgmtReport{Start,End}Date`; a per-service pane: legal → a stage-picker with **active preselected**, completed selectable, **pending LOCKED** 🔒; hours/fixed → an explanatory line; the "unassigned" note; a PDF/Excel segmented footer + email/generate).
- **M3 — DA-1 + DA-2 preserved.** Format radios keep `name="mgmtReportFormat"`, read root-scoped (`root.querySelector('input[name="mgmtReportFormat"]:checked')`), never the global `reportFormat`. A legal service with no active/completed stage carries `stage:''` → `_validateSelection` refuses to generate it.
- **M4 — injector safety.** The report tab emits **NO** `.management-*` classes (grep-verified — only the docblock mentions the word). The rail rows are `.cm-rail-row`; the detail is `.report-*`. `buildManageDetail` + the manage-panel injector DOM are untouched.
- **M5 — "unassigned hours" is the REAL number, never a fake (ADMIN SAFETY).** Computed from `dataManager.getClientTimesheetEntries(client.fullName, start, end)` over the current date range; "unassigned" = Σ minutes of entries matching NO client service (orphan), ÷60; `_entryMatchesAnyService` mirrors `ReportGenerator.collectReportData`'s matchService fields (trimmed: serviceId / service-as-id / service-as-name / serviceName / any legal stage id). Returns **null** (note hidden) when uncomputable (no dataManager / no reader / throw / non-array) — NEVER 0. The note is hidden when the value is 0 or null. Recomputes on date-range change.
- **M6 — dataManager wiring + engine calls unchanged.** `ClientManagementModal._switchTab` now calls `window.ReportTab.render(this.currentClient, reportPanel, this.dataManager)` (one line). `_generate` → `window.ReportPreview.showForFormData(getFormData())`; `_email` → `window.ReportGenerator.generateAndEmail(getFormData())` — verbatim.
- **M7 — tokens + accessibility (Design Bar).** The new `.report-*` CSS uses design tokens only (no hardcoded color/spacing literals); RTL logical properties; transitions via `--transition-*` (reduced-motion safe); `:focus-visible` on `.report-stage`, `.btn-quick-date`, and the segmented options; WCAG AA contrast. Every interpolated value in ReportTab is escaped at the sink via `window.escapeHtml`.
- **M8 — gates.** Full admin-panel vitest green (458, was 453); ESLint 0 errors (pre-existing `no-console` warnings in `ClientManagementModal.js` are untouched lines); stylelint 0; `node --check ReportTab.js` clean; `?v=` bumped on `clients-modals.css` + `ReportTab.js` + `ClientManagementModal.js`.

## SHOULD

- **S1** — the retargeted `report-tab.test.ts` keeps the 9-key contract + DA-1 pins + the `name="mgmtReportFormat"` count + the clients.html HTML pins; only the DOM-shape assertions move from the old `.report-service-card` grid to the rail rows; NEW cases cover the locked-pending stage, the DA-2 stageless refusal, and the unassigned-hours note (shown / hidden / no-dataManager).

## PRODUCT-GRADE GATES (expected)

- **G1** PASS — Hebrew customer-facing text; ratios/notes numeric + `.toFixed(1)`-guarded (no `undefined`/`NaN` — `num`/`mins` coerce, the note only renders when `hours>0`); the resilience fallbacks are Hebrew ("טעינת מנוע הכרטיסים נכשלה" / "אין שירותים להצגה"); no throw on a missing renderer/dataManager.
- **G2** PASS — Rollback = a single `git revert` restores the flat-form `ReportTab.js` + the one CM line + the CSS + the `?v=`.
- **G3** N/A — read-only display; the report GENERATION write path (ReportGenerator) is downstream and untouched.
- **G4** PASS — the vitest suite drives the real ReportTab against the real DOM and proves the customer scenario: select a service in the rail → (legal) pick a stage → `getFormData` yields the byte-identical 9-key payload; the unassigned note shows/hides with the ledger stub.
- **G5** PASS — all customer-facing strings Hebrew.
- **G6** PASS-with-declaration — **BEHAVIORAL (display + interaction):** the report stage-selection changes from a flat one-card-per-stage grid to a rail (per-service) + a detail stage-picker (same selectable set: active+completed; pending now shown-but-locked); a NEW read-only "unassigned hours" note is added. NO data/contract change — `getFormData` is byte-identical, `ReportGenerator`/`ReportPreview`/the callables and the report OUTPUT are unchanged; no count/filter/aggregate. No migration.
- **G7** N/A — no auth/PII/permissions; display of data already in the modal.
