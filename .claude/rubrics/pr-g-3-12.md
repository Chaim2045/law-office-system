# Rubric — PR-G.3.12: apply "no work on holiday eves" policy in calendar.js

**Scope:** office policy change announced 2026-05-20 (`אין עבודה בערב חג`). Holiday eves are now full non-working days (previously half-day). Backend `functions/shared/calendar.js` + tests + frontend embedded fallback in both apps' `holidays-cache.js` (byte-identical copies).

**Policy origin:** Tommy explicit instruction during PR-G.3.x planning. Was discussed in G.3.2 but never landed in code (oversight discovered during G.3.7 follow-up smoke).

## MUST (8) — blocking

1. **Eve classifier returns `isWorking:false, isHalfDay:false`.** `functions/shared/calendar.js:142-148` returns `{ type: 'eve', isWorking: false, isHalfDay: false, eveOf: basename }`. *Evidence:* grep shows the new values; `eveOf` retained for UI distinction.

2. **All existing tests still pass.** No regression in Jest or Vitest. *Evidence:* CI green. Jest 551+ (was 549 + 2 new). Vitest 259 pass / 2 skipped (unchanged).

3. **Renamed/updated test "Erev Pesach is half-day".** Asserts new policy: `isHalfDay:false, isWorking:false`. *Evidence:* `functions/tests/calendar.test.js` test renamed to "Erev Pesach is FULL non-working day (PR-G.3.12 policy)".

4. **New test: all eves are non-working.** Asserts EVERY eve in the year returns `isWorking:false, isHalfDay:false`. *Evidence:* test "all eves of closed holidays are non-working (PR-G.3.12)" iterates and asserts.

5. **New test: `isWorkday('2026-04-01') === false`.** Erev Pesach must be non-working day via top-level API. *Evidence:* test "isWorkday: Erev Pesach → false (PR-G.3.12)" passes.

6. **Frontend embedded fallback updated in BOTH copies.** `apps/user-app/js/shared/holidays-cache.js` AND `apps/admin-panel/js/shared/holidays-cache.js` — all 7 eve entries in `EMBEDDED_FALLBACK_2026` flipped to `isWorking:false, isHalfDay:false`. *Evidence:* grep `'isHalfDay':true` returns no hits in either file.

7. **Byte-identical copies preserved.** `diff -q` of both files returns no output. *Evidence:* `BYTE_IDENTICAL_OK`.

8. **No backend cron / functions deploy required for policy to take effect.** After merge, Cloud Function `holidaysCalendarSync` will re-deploy on next production-stable merge; until then it runs OLD code which still produces `isWorking:true, isHalfDay:true` for eves. DEV behavior post-merge: cron in CF still old; data still old; frontend reads old. BUT scripts run locally (after pulling main) will produce NEW policy. **Cron force-run / functions redeploy is the action that flips DEV data.** *Evidence:* this is a process note, not code.

## SHOULD (3) — non-blocking

1. **Comment at fix site cites policy date + Hebrew quote.** Explains WHY eve is flipped, not just what. *Evidence:* JSDoc-like comment at calendar.js:140-144 mentions "office policy 2026-05-20" and Hebrew quote.

2. **Comment at `buildHolidaysMap` flags the now-dead half-day tiebreaker.** Defensive guard retained. *Evidence:* updated comment at line ~241.

3. **`eveOf` field retained for UI distinction.** Consumers can still show "Erev Pesach" labeling differently from "Pesach I", even though both are closed. *Evidence:* eve still includes `eveOf` per code at line 145.

## Out of scope

- ESLint enforcement of policy (defer)
- Frontend `WorkHoursCalculator` `monthlyGoal` recalculation due to fewer working days — covered by existing logic that reads `!h.isWorking || h.isHalfDay` (eves now match first clause; same outcome). No code change needed; behavior auto-adjusts.
- Cron redeploy + force-trigger (operational task #10 still pending)
- BC `data.holidays` cleanup (PR-G.3.5)

## Test outputs required

- Jest: PASS (551+ tests, +2 new for eve policy).
- Vitest: PASS (259/2 skipped unchanged).
- Lint: 0 errors.
- Diff verification: `diff -q apps/user-app/js/shared/holidays-cache.js apps/admin-panel/js/shared/holidays-cache.js` returns nothing.
