# Rubric — PR-R3: report tab → "reading pane" polish (identity band + segmented presets + live caption + מותאם disclosure + quiet note)

**Track:** Admin modal design-polish. After PR-R2 shipped the report tab as master-detail, this PR brings the DETAIL pane up to the product-owner-locked "reading-pane" design (mockup at `<scratchpad>/report-polish-mockup.html`): a per-service identity band, the 5 quick-date presets restyled as one segmented sliding control, a live resolved-date caption, the native date inputs moved behind a "מותאם" disclosure, and the "unassigned hours" note quieted from a filled box to a hairline.
**Scope:** **frontend-only, admin-critical.** 4 files — `ReportTab.js` (`_shellHtml` reorder + identity band in `_renderServiceDetail` + segmented-thumb/caption/disclosure wiring), `clients-modals.css` (additive `.report-*` classes in existing design-system tokens + quiet-note restyle + dead `.report-date-bar` removal), `clients.html` (`?v=` bump on the 2 changed assets), `report-tab.test.ts` (new tests, all existing kept). `getFormData` / the report engine / the manage tab / rules / callables — **UNTOUCHED**.
**Type:** admin-critical UI change (>100 lines → devils-advocate MANDATORY §3.8.4). Display + interaction change; **no data/contract change** — the report OUTPUT is unchanged (same `getFormData` → same engine).

## MUST (all required for PASS)

- **M1 — `getFormData()` byte-identical (THE report-engine contract).** The method body is unchanged (verified: `git diff origin/main` shows zero hunk in `getFormData`). Presets and the (moved) native date inputs still write to `#mgmtReportStartDate`/`#mgmtReportEndDate`; format radios stay `name="mgmtReportFormat"` read root-scoped; the `_selection` payload (service = `card.name` for hours/fixed, `getStageName(stage.id)` for legal) is byte-preserved.
- **M2 — reading-pane structure (mockup parity, Path-A colors).** `.report-detail` renders, in order: an identity band (per selected service, via `_renderServiceDetail`) → `.report-period` (segmented `#mgmtReportPresetSeg` with a sliding `.report-preset-thumb` over the 5 existing `.btn-quick-date[data-range]` buttons — SAME data-range values/labels; a `.report-dateline` with `#mgmtReportResolvedRange` + a `#mgmtReportCustomToggle`; a collapsed `.report-custom-dates` holding the moved `#mgmtReportStartDate`/`#mgmtReportEndDate`) → the note → the existing footer. Every color/spacing/radius/shadow/transition is an EXISTING `design-system.css` token; the ONLY hardcoded literal is the approved feature-local `--report-surface:#fff` (white, absent from design-system, mirrors the existing `--report-on-fg` local token).
- **M3 — injector safety.** The report tab emits ZERO `.management-*` classes (`grep -c 'management-' ReportTab.js` = 0, incl. comments). New classes are `.report-*`. The manage-tab injectors (`ServiceOverdraftResolution`/`AddPackageToStage`) never match here.
- **M4 — date-input invariants.** The native date inputs KEEP their ids and stay in `this._root` at all times (CSS-collapsed via `.report-custom-dates`, NEVER conditionally rendered/removed). No `new Date(startInput.value)`/`new Date(endInput.value)` anywhere (the Hebrew caption parses via `.split('-')`); the `_computeUnassignedHours` LOCAL-midnight/end-of-day window (`parseLocalDate`, `23,59,59,999`) is intact.
- **M5 — identity band correctness.** Icon/badge per type (hours=`fa-clock`/שעות, fixed=`fa-dollar-sign`/מחיר קבוע, legal=`fa-gavel`/הליך משפטי). Hours only: an inline-end stat (`used / total ש׳ · נותרו X` or `· חריגה X` in red when `card.hoursRemaining < 0`) with the figure capped at a normal size (NOT a hero) + a 4px meter whose fill class is `over`(≤0→red)/`high`(≤10→orange)/`good`(else→green). Legal → no band meter, the existing `.report-stage-list` is the body. Fixed → no meter, the existing explanatory note. `card.hoursRemaining` is read directly (confirmed present + unclamped in ServiceCardModel).
- **M6 — quiet note.** `.report-unassigned-note` is a hairline (flex + `--gray-500` text + small `--orange-dark` icon + `--gray-900` bold number), NOT a filled/bordered box. Its show/hide behavior (`_renderUnassignedNote`, hidden when 0/null) is unchanged.
- **M7 — Design Bar + gates.** Tokens only (no hardcoded color/spacing literals beyond `--report-surface`); motion via `--transition-*` (reduced-motion safe); `:focus-visible` on every interactive (`.btn-quick-date`, `#mgmtReportCustomToggle`, format options, actions); RTL logical properties (the sliding thumb uses physical `left`+`offsetLeft` by design — RTL-correct). Full admin-panel vitest green (471, was 460), ESLint 0 errors (pre-existing `no-alert` warnings untouched), stylelint 0 problems, `node --check ReportTab.js` clean, `?v=` bumped on `clients-modals.css` + `ReportTab.js` only.

## SHOULD

- **S1** — the added tests cover the new surfaces: identity band per type, the 3 meter thresholds (incl. `חריגה 5.0`), a preset click still yields the byte-identical 9-key `getFormData`, the מותאם disclosure (inputs present before AND after), the live resolved caption, the quiet-note structure, and an injector-safety grep. All 17 pre-existing report-tab tests kept green.

## PRODUCT-GRADE GATES (expected)

- **G1** PASS — Hebrew customer-facing text; numbers `num()`-coerced + `.toFixed(1)`-guarded (no `undefined`/`NaN`); resilience fallbacks Hebrew; no throw on missing renderer/dataManager.
- **G2** PASS — Rollback = a single `git revert` restores the R2 `_shellHtml`/detail + the CSS + the `?v=`.
- **G3** N/A — read-only display; the report GENERATION write path is downstream and untouched.
- **G4** PASS — the vitest suite drives the real ReportTab against the real DOM and proves the customer scenario: pick a service → (legal) pick a stage / (hours) see the band → pick a preset → `getFormData` yields the byte-identical 9-key payload; the note shows/hides.
- **G5** PASS — all customer-facing strings Hebrew.
- **G6** PASS-with-declaration — **BEHAVIORAL (display + interaction):** the date UI restructures from an always-visible bar to a segmented preset control + a resolved caption + a "מותאם" disclosure; a per-service identity band is added; the unassigned note is quieted. **NO data/contract change** — `getFormData` is byte-identical, the engine/callables/output are unchanged; no count/filter/aggregate. No migration.
- **G7** N/A — no auth/PII/permissions; display of data already in the modal.
