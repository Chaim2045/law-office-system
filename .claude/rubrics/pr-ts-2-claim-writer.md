# Rubric — PR-TS-2: migrate `functions/shared/claim-writer.js` → TypeScript (גל-3ה, module 2 of the safe tier)

**Scope:** Second JS→TS migration, applying the **A′ template proven in TS-1** (#496). Migrates `claim-writer.js` — a PURE, backend-only read-merge-write primitive for Auth custom-claim role edits (2 functions, no I/O, no deps, `.d.ts` already existed, 3 consumers, **an existing behavioral test**). **Reorder note:** the map's §7 order put service-classification #2, but a read-only check found it is the MOST entangled safe-tier module (2 CJS copies + 2 IIFE adapters + a 5-way sync-test); claim-writer is the true logger-analog (single file, `.d.ts`, zero cross-app mirror) → done next as the cleanest second proof of A′. service-classification + validators (both with cross-app mirrors) follow with their specific care. **Same safe tier Haim approved.**

## Why TS-2 is lighter than TS-1
All A′ infrastructure landed in TS-1 and is REUSED unchanged: the scoped `functions/shared/tsconfig.json` (just +1 include line), the `functions/shared/ts/**` ESLint block, the `.gitattributes` LF discipline, and the CI **build-drift guard** (`git diff --exit-code` on `functions/shared/*.{js,d.ts}`) — which now automatically covers claim-writer. No package.json / eslint.config / workflow change.

## MUST
- **M1 — output byte-PATH-identical + zero consumer changes.** Compile emits `functions/shared/claim-writer.js` + `.d.ts` at the SAME paths (M, not D+A). The 3 consumers resolve unchanged: 2 legacy JS destructured `const { mergeRoleClaim, removeRoleClaim } = require('../shared/claim-writer')` (`auth/index.js`, `master-admin-wrappers.js`) + 1 TS `import { … } from '../shared/claim-writer'` (`sync-role-claims.ts`). No require/import path edited.
- **M2 — named-export surface preserved.** Emitted `.js` = `exports.mergeRoleClaim` + `exports.removeRoleClaim` + `__esModule` (verified). Destructured `require` + named `import` both work. NOT `export =`/`module.exports=`. Generated `.d.ts` declares `ClaimMap` + both functions with the same signatures as the old hand-written contract.
- **M3 — 1:1 behavior port (verified).** `mergeRoleClaim`: throws on non-string/empty role, else `{ ...(existing||{}), role }` (preserve every field, set role). `removeRoleClaim`: `delete next.role` on a shallow copy (preserve every field, drop only role). Byte-for-behavior identical to the old JS. This is a SECURITY primitive — the migration changes NO logic.
- **M4 — deterministic output.** `ClaimMap = Record<string, unknown>` + both function signatures explicitly typed → the generated `.d.ts` is env-independent (no inferred-namespace widening like logger's `_raw`). A fresh `build:ts` in CI produces byte-identical committed output (the drift-guard enforces it). Verified locally: re-running the compile left `logger.js` UNCHANGED (multi-module tsconfig is deterministic).
- **M5 — behavior pinned by the EXISTING test.** `functions/tests/claim-writer.test.js` (legacy-js jest) already exercises both functions and runs against the compiled `../shared/claim-writer` → it is the migration safety net (G4). No new anchor test needed (unlike logger, which had none).
- **M6 — no rootDir/eslint regression.** Source at `functions/shared/ts/claim-writer.ts` (not `shared/`) → src-ts resolves `../shared/claim-writer` to the generated `.d.ts` (verified: no TS6059). The TS-1 ESLint block `functions/shared/ts/**/*.ts` covers it (no parse error). LF pinned.
- **M7 — exec-log maintained.** `docs/HEALTH-MAP` exec-log gains the TS-2 row + flips TS-1 #496 to ✅ merged. Additive.

## PRODUCT-GRADE GATES
- **G1/G3/G5** N/A — pure data-transform primitive; no customer strings, no data mutation (it computes an object; the CALLER writes it), no user-facing path.
- **G2 (rollback):** `git revert <sha>` restores the hand-written `claim-writer.js`/`.d.ts`, removes `ts/claim-writer.ts`, reverts the tsconfig include + gitattributes. Trivial.
- **G4** — the pre-existing `functions/tests/claim-writer.test.js` proves the scenario (grant preserves other claims; revoke drops only role) against the compiled output; runs in CI.
- **G6 — NO breaking change (declared).** Output byte-path-identical + surface-preserved → every consumer resolves + compiles unchanged. No schema/API/route/default change.
- **G7 (security):** N/A-with-note — claim-writer IS an Auth-claim primitive, but this PR is a **pure refactor with zero logic change** (no new `setCustomUserClaims` call, no rule change → §3.8.4 auth-trigger not met). The read-merge-write invariant (preserve non-role claims) is unchanged and still pinned by the existing test.

## Anti-premature-closure
- **devils-advocate + security-access-expert RUN (quality-first upgrade, 2026-07-30):** the rubric originally declared a devils-skip (A′ mechanism already devil's-advocated in TS-1). Per Haim's explicit quality emphasis ("הדגש שלי על איכות העבודה"), the skip was **broken** because claim-writer is an Auth-claim SECURITY primitive → both agents ran read-only on the staged diff. **Result:** security-access-expert=**PASS** (byte-for-behavior-identical vs `git show HEAD`; no new PII/logging/import/IO; `.d.ts` exposes exactly `ClaimMap`+2 functions; all 3 consumers resolve unchanged; public-repo clean). devils-advocate=**GO-WITH-CHANGES** (the "zero logic change" claim held under attack — a fresh `tsc` compile is byte-identical to the committed emit, and every security path was runtime-verified; no blocking attack survived).
- **2 non-blocking folds taken** (both cheap, both raise robustness on a security file):
  1. **`newLine: "lf"` pinned** in `functions/shared/tsconfig.json` → the CI build-drift guard is now compiler-enforced (no longer dependent on `.gitattributes` renormalization); benefits logger too. Verified a true no-op on committed content (recompile → empty `git diff` on all 4 emitted files).
  2. **Non-string-role throw assertions** added to the existing `functions/tests/claim-writer.test.js` (`123` / `null` / `{}`) → pins the `typeof role !== 'string'` fail-closed branch so a future guard "simplification" can't silently let a numeric role claim through. Verified all 5 guard cases throw against the compiled module.
- **standing rule adopted:** every security/auth primitive in the safe tier gets devils-advocate + security-access-expert even on a "pure" 1:1 migration.
- **Full local verification (better than TS-1):** claim-writer is PURE (no firebase-functions import) → `tsc` compiled it cleanly LOCALLY with zero TS2307; emitted surface + deterministic `.d.ts` verified locally; only the jest run (needs functions node_modules) is CI-side.
- **גל-3ה safe tier progress:** TS-1 logger ✅ → TS-2 claim-writer (this) → then validators + service-classification (both have cross-app mirrors → handled with their own investigation), then REASSESS before the large financial-core writers.
