# Rubric — PR-T2-1: extract the identity band to the shared renderer (convergence)

**Track:** Track-2 (reading-pane for the management card), PR-1 of 4. This is the **convergence-rule** PR: the report tab's per-service identity band is EXTRACTED into ONE shared renderer that both tabs will use — **not copied** into a second `.management-identity-*` (copying would re-diverge the exact thing the U0–U7 unification killed). PR-1 touches the REPORT surface only (the safe proving ground); the management card adopts the shared band in PR-2.
**Scope:** frontend-only, DEV. Move `identityBandHtml` + `meterStatus` from `ReportTab.js` → `UnifiedServiceCard.buildIdentityBand` (exposed in `api`), class prefix `report-identity-*` → **neutral `.usc-identity-*`**; repoint ReportTab's 5 call sites to `window.UnifiedServiceCard.buildIdentityBand(card)`; rename the CSS block + the test assertions in lockstep; `?v=` bump on the 3 touched assets. **Zero visual/behavior change** — the report renders byte-identical (only class names + code location change).
**Type:** SSOT-preserving extraction. LIGHT→MEDIUM. **No devils-advocate** — frontend-only, no rules/claims/migration, no new dependency, and the output is byte-identical (test-proven); DA is reserved for PR-2 (management adopt + calm overdraft) and PR-3b (Floating UI new dependency), per the frontend-ui-expert scoping.

## MUST (all required for PASS)

- **M1 — verbatim extraction (byte-identical output).** `buildIdentityBand(card)` produces the SAME HTML as the old `ReportTab.identityBandHtml(card)` except the class prefix `report-identity-*` → `usc-identity-*`: same icon/badge routing (fa-gavel/הליך משפטי, fa-dollar-sign/מחיר קבוע, fa-clock/שעות), same meter/stat gate (`!isLegal && !isFixed && total>0`), same STRICT `meterStatus` (rem<0→over, ≤10→high, else good; rem===0→high never red), same `toFixed(1)` + clamped `pct`, same `חריגה`/`נותרו` text, name via `esc()`. `bandNum` coerces exactly like the source `num()`. **Proven:** the 38 `report-tab.test.ts` cases (band-per-type + threshold classes over/high/good + `total≤0` → no meter) pass against the renamed `.usc-identity-*`.
- **M2 — convergence, NOT copy.** The band lives ONCE — `UnifiedServiceCard.buildIdentityBand` (in `api`). ReportTab's local copy is DELETED (`grep identityBandHtml ReportTab.js` = 0, `grep meterStatus` = 0) and the 5 call sites call the shared builder (`grep 'window.UnifiedServiceCard.buildIdentityBand(card)'` = 5). No `.report-identity-*` remains anywhere (`grep report-identity` across admin-panel+tests → only the renamed occurrences, 0 in JS).
- **M3 — neutral, injector-safe namespace.** `.usc-identity-*` collides with nothing (`grep` = 0 against `.management-*`/`.report-*`/`.cm-*`). ReportTab still emits **0** `.management-*` (`grep management- ReportTab.js` = 0). The management renderer + the injector contract are UNTOUCHED in this PR (no `buildManageDetail` change).
- **M4 — CSS + test renamed in lockstep.** `clients-modals.css` `.report-identity-*` → `.usc-identity-*` (that block only, stylelint 0); `report-tab.test.ts` assertions renamed; no other file references `report-identity`.
- **M5 — `?v=` bumps.** `clients-modals.css` + `UnifiedServiceCard.js` + `ReportTab.js` all bumped to `20260805-t2-1-identity-extract`. `clients-fluent.html` unaffected (loads neither JS — verified). Load order preserved (UnifiedServiceCard `:504` before ReportTab `:508`, so the shared builder exists when ReportTab runs).
- **M6 — gates.** `node --check` OK (both JS); eslint **0 errors** (the `no-restricted-syntax` warning on the legal predicate is suppressed via the SAME disable the extracted source carried — switching to the `BUSINESS_RULES` predicate could change classification, out of scope for a byte-identical extract; the 5 pre-existing `no-alert` warnings in ReportTab are untouched); stylelint 0; **93 tests green** (report-tab 38 + preview-contract 13 + modal-unification-report 11 + management-contracts 25 + manage-detail-equality 6).

## PRODUCT-GRADE GATES (expected)

- **G1** PASS — no customer strings changed (band text identical); no undefined/NaN/error path.
- **G2** PASS — Rollback = single `git revert` (restores the local band in ReportTab + the `.report-identity-*` CSS/test + `?v=`).
- **G3** N/A — read-only display; no write/update/delete path.
- **G4** PASS — byte-identity proven by the 38 report-tab tests (band per type + threshold classes) + manage-detail-equality (management contract intact). jsdom can't assert pixels; manual DEV smoke after merge: open the report tab → the per-service band looks identical to today (icon+name+badge, meter color, stat).
- **G5** PASS — Hebrew band text unchanged (הליך משפטי / מחיר קבוע / שעות / נותרו / חריגה).
- **G6** PASS (no breaking change) — `getFormData` untouched; the report output is byte-identical; only the class prefix + the code's home changed. No data/contract/count/aggregate change.
- **G7** N/A — no auth/PII/permissions surface.
