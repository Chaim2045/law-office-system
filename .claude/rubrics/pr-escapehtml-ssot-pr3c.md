# Rubric — PR-ESCAPEHTML-SSOT-PR3c

**Title:** escapeHtml SSOT consolidation — PR3c: WorkloadCard onclick→delegation refactor + delete `sanitize` + dead-code cluster removal (closes the dedup track)
**App:** Admin Panel · **Env:** DEV · **Frontend-only, behavioral refactor (onclick→addEventListener) + SSOT routing + dead-code deletion, admin-critical**

**Scope:** PR3c of the escapeHtml-dedup PR3 (Haim-approved decomposition 2026-06-21 — "PR3a now, PR3c after"). The behavioral half PR3a deferred. It does THREE things on `apps/admin-panel/js/workload-analytics/WorkloadCard.js`:

1. **Fix the live drawer-open onclick → event delegation.** The per-row inline `onclick` interpolated `employee.email` into a JS-string — an apostrophe email (`o'brien@x.com`) broke that onclick (`&#039;` HTML-decodes back to `'` before the JS engine, so escaping alone could never fix it — only a data-attr + `addEventListener` reading `dataset.email` decodes safely). Replaced with: `data-email="${window.escapeHtml(employee.email)}"` on the row + a SINGLE delegated `click` listener on the persistent `#workloadContent` container (only its `innerHTML` is re-rendered, never the element itself), reading `row.dataset.email` verbatim, guarded by `__wlcRowDelegated` against a double-bind.
2. **Route `sanitize` to the SSOT, then DELETE it.** All `this.sanitize(` / `workloadCard.sanitize(` call-sites → `window.escapeHtml(` directly (10 sites); the `sanitize(text) {…}` thin wrapper is then removed entirely (no local escaper remains — `escape-html.js` is loaded before WorkloadCard on workload.html since PR3a).
3. **Delete the dead-code cluster (~790 lines, 3017→2224).** 12 dead methods + 3 dead `window.*` globals, all proven zero-live-callers: `extractReasonsChips`, `renderManagerSummary`, `renderManagerInsightRow`, `renderDetailedSections`, `renderEmployeeCard`, `renderCriticalAlertsSection`, `renderTaskQualityCategory`, `renderWeeklyBreakdownCategory`, `renderRiskyTasksCategory`, `renderAllAlertsCategory`, `renderWeeklyBreakdownContent`, `renderRiskyTasksContent` + `window.toggleCategory` / `window.toggleBreakdown` / `window.toggleAllPeakTasks`. **The premise-overturning discovery (re-checkpointed with Haim):** `renderEmployeeCard` — which contained the category-toggle subsystem (5 of the 6 onclick sites the original PR3c was scoped to "refactor") — has ZERO callers, so those 5 onclick sites were DEAD, not live. Scope reduced to "fix the 1 live onclick + delete the dead." The deletion also swept 3 pre-existing orphans (`renderManagerSummary`, `extractReasonsChips`, `renderManagerInsightRow`) in the same contiguous dead cluster.

**Also:** the obsolete PR3a routing assertion (`WorkloadCard.sanitize delegates to the SSOT` — pinned the now-deleted `sanitize(text) {` signature) is updated to the PR3c end-state (no wrapper), and its now-unused `escaperBody` helper removed. `?v=` on workload.html bumped to `?v=20260621-escapehtml-pr3c`.

**Excluded (intentional, in scope to KEEP):** the 7th inline onclick `onclick="window.toggleEmployeeGroup('${groupId}')"` — `groupId` is `group-${status}` (a system-controlled status enum, never user-text) → no XSS, no JS-string hazard; refactoring it is out of scope and allow-listed in the guard test. `getConfidenceClass` + ALL live render methods (renderNarrativeGridRow, renderEmployeeListRow, renderDrawer, attachEventListeners, dateToYYYYMMDD, …) KEPT.

## MUST (block on FAIL)
- **M1** — the live per-row drawer-open inline `onclick` is GONE; the row carries `data-email="${window.escapeHtml(employee.email)}"`; a single delegated `click` listener on `#workloadContent` reads `e.target.closest('.narrative-grid-row').dataset.email` and calls `window.workloadDrawer.open(email)`. An apostrophe email (`o'brien@x.com`) now opens the correct drawer (the PRE-EXISTING onclick break is FIXED, not merely escaped).
- **M2** — the delegation is bound ONCE on the persistent container (guarded by `__wlcRowDelegated`); a re-render (which replaces only `innerHTML`, never the element) does NOT re-bind → exactly ONE `workloadDrawer.open` per row click. A click outside any `.narrative-grid-row` is ignored.
- **M3** — every `sanitize` call-site routes to `window.escapeHtml` directly AND the `sanitize(text) {…}` method is deleted (zero `sanitize` references remain in the file). "Route, never copy" — no local escaper survives.
- **M4** — every deleted method/global is justified by PROVEN deadness (zero live callers in the file + zero external `window.*` reads), NOT by name or assumed-duplicate ("verify content hash, never dedup by name" → here: verify zero-callers, not equality). Post-deletion: 0 references to any deleted name, `node --check` SYNTAX OK. All live render methods PRESERVED. (Note: `getConfidenceClass`, interleaved in the deleted region, is itself dead-but-out-of-cluster — it was conservatively KEPT, not deleted; the sweep targeted the contiguous category-toggle dead island, not every dead symbol in the file. Verified by devils-advocate.)
- **M5** — the `toggleEmployeeGroup('${groupId}')` onclick is intentionally KEPT (system-controlled `groupId`, no XSS) and the guard test allow-lists exactly it; no other inline `on*="...${...}..."` handler remains in the source.
- **M6** — behavior DECLARED (G6): for benign emails the delegated handler is functionally identical to the old onclick (same drawer opens); for apostrophe emails it now works (was broken). Deleting the dead cluster changes NOTHING observable (zero live callers). No data/schema/rule/route/return-shape contract changes.
- **M7** — tests prove it: source guards (no disallowed inline `on*=`+`${}`; `sanitize`/`renderEmployeeCard`/the 3 dead globals removed; `__wlcRowDelegated` + `closest('.narrative-grid-row')` present) + happy-dom behavioral (verbatim apostrophe email from `dataset.email`; double-bind guard = one open/click; outside-row ignored). The updated PR3a test + full admin-panel suite green; `node --check` OK.

## SHOULD
- **S1** — the delegation block carries a comment explaining WHY (the onclick JS-string apostrophe hazard, attach-once on the persistent container, double-bind guard).
- **S2** — workload.html `?v=` bumped to `?v=20260621-escapehtml-pr3c` (escape-html.js already loaded before WorkloadCard.js since PR3a — verified, unchanged).
- **S3** — the PR body notes the dead-code deletion also removed 3 pre-existing orphans (part of the contiguous dead cluster), and that the original "6-onclick refactor" was reduced after the dead-`renderEmployeeCard` discovery (re-checkpointed).

## Test plan
`tests/unit/admin-panel/workloadcard-onclick-refactor.test.ts` (7 tests): source guards — (1) no inline `on*=` with `${...}` except the allow-listed `toggleEmployeeGroup('${groupId}')`; (2) `sanitize` / `renderEmployeeCard` / `window.toggleCategory|toggleBreakdown|toggleAllPeakTasks` all removed; (3) `__wlcRowDelegated` + `e.target.closest('.narrative-grid-row')` present — and happy-dom behavioral — (4) delegated click → `workloadDrawer.open` with the verbatim apostrophe email `o'brien@x.com` read from `dataset.email`; (5) the FULL escape→parse→`dataset` round-trip executed end-to-end with `&` (a char happy-dom decodes — proving the join step the apostrophe case can only argue, since happy-dom under-decodes the numeric `&#039;` that real browsers decode); (6) double-bind guard = exactly one open per click; (7) a click outside a row is ignored. Plus the updated `escapehtml-ssot-pr3a-routing.test.ts` (the obsolete sanitize assertion → the PR3c end-state). Full admin-panel suite **190/190**. `node --check` SYNTAX OK. Local ESLint via `eslint --config <main>/eslint.config.js`: **0 errors** on the changed files (warnings = pre-existing no-console/no-alert in untouched WorkloadCard lines + standard test-file `any`/non-null, within the 2200 CI budget). **devils-advocate RUN (MANDATORY — admin-critical behavioral refactor + >100-line deletion, §3.8.4)** — verdict cited in the PR body.

## Rollback
`git revert <merge-commit>` + redeploy (frontend; Netlify). No data migration, no schema/rule/CF change. Reverting restores the inline onclick + the `sanitize` wrapper + the ~790 deleted lines + the prior `?v=`.

## PRODUCT-GRADE GATES
- **G1 PASS** — the delegated handler null-guards (`row && row.dataset.email && window.workloadDrawer`); no TypeError on a missing drawer/email; `window.escapeHtml` is defined at render (PR3a load-order, unchanged). The apostrophe-email break (a latent G1 "button does nothing / opens wrong drawer") is FIXED.
- **G2 PASS** — `git revert` rollback (code-only, no data/schema/rule).
- **G3 N/A** — no data mutation (display-render refactor + dead-code deletion).
- **G4 PASS** — behavioral happy-dom tests prove the customer scenario (a row click — including an apostrophe-email row — opens the right drawer, exactly once) + source guards; full admin suite green.
- **G5 PASS** — no customer-facing strings added/changed; the escaper maps only `& < > " '`, Hebrew passes through.
- **G6 PASS (declared)** — behavioral change: per-row interaction moves from inline onclick to delegated listener (functionally identical for benign input, fixed for apostrophe input). Deleting the dead cluster is observably inert (zero live callers). No data/contract/route change.
- **G7 PASS** — XSS/correctness hardening: removes the last JS-string-interpolation onclick in WorkloadCard (the data-attr+delegation pattern decodes safely); `sanitize` consolidated onto the audited 5-entity SSOT; the one kept onclick is provably XSS-safe (system-controlled `groupId`). No PII/auth/rules surface touched (frontend display only).

VERDICT: PASS
