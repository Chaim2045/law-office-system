# Rubric — PR-SHARE-6: unify service-card-renderer (גל-3ג)

**Scope:** The highest-stakes shared-mechanism PR — unifies the drifted admin/user `service-card-renderer.js` (59-line diff, 4 hunks) into ONE canonical via APP_CONTEXT injection. **devils-advocate DONE = GO-WITH-CHANGES** (its corrections folded in below). Preserves each side's existing card behavior; the only INTENDED runtime changes are H1 (user escape upgrade) + H3 (admin title). Confidentiality-safe (verified).

## MUST
- **M1 — one canonical, registered.** `shared-web/src/modules/service-card-renderer.js` (sentinel default `'user'`); registered in emit MODULES; no other pair touched.
- **M2 — H1 (escape) upgrade, no new dependency.** Canonical `const escapeHtml = window.escapeHtml || <inline 5-entity fallback>`. Admin uses the SSOT (`escape-html.js` loaded before it on `clients.html:566`); user uses the inline 5-entity fallback (upgrade from its old 3-entity temp-div). Fallback escapes & (first) < > " '. No `escape-html.js` added to user-app.
- **M3 — H2 (remaining-hours) canonical = admin's "0" (Haim-decided from a prod probe: 39/40 identical; the 1 residual was 0.05h).** ACCURATE FRAMING (devils-advocate correction): this aligns the renderer's LOCAL `calculateRemainingHours` to admin's no-fallback form. It is **functionally inert in the user app** — the user-app's live `window.calculateRemainingHours` is owned by `core-utils.js`/`calculators.js` (which keeps the fallback), and the renderer's own assignment is guarded (`if(!window.calculateRemainingHours)`). Admin (no core-utils) uses the renderer's version — unchanged from admin's prior behavior. So H2 is a consistency alignment of the module's local copy, NOT a user-runtime change.
- **M4 — H3 (title) canonical = user's real-name title.** `title = service.name || 'שירות שעות · נותרו X'`, subtitle 'תוכנית שעות'. This CHANGES the admin card title (was the generic constant) → a declared admin-trusted DISPLAY change. Verified NOTHING keys off the old title/subtitle (no admin selector/count/filter/test/analytics reads the card DOM title — the `'תוכנית שעות'` grep hits are separate render paths / a `service.type` compare, not this card). Cosmetic, no count/filter/status moves.
- **M5 — H4 (§7.6-class confidentiality) fail-secure + ACCURATE claim.** `const SHOW_FINANCIALS = (APP_CONTEXT === 'admin')` (const, strict-eq, no coercion, default-deny). Admin branch renders `₪price`+hours+entries; staff branch renders only the "שירות קבוע" badge (each side's CURRENT card behavior, byte-for-byte, now selected by the flag). **ACCURATE FRAMING (devils-advocate correction):** this PRESERVES the existing "employee card shows no ₪ amount" behavior — it does NOT newly achieve system-wide confidentiality. Product reality (probe-confirmed + Haim): employees are NOT meant to see the ₪ amount, and they don't — no employee UI renders it (`cases.js` reads a doc-level `fixedPrice` that is empty on all 13 fixed cases → shows "לא הוגדר"; the real amount lives at `services[].fixedPrice` in the world-readable `clients` doc but no employee surface renders it — a broader G7 data-access item, not this PR). The fail-secure flag is the mechanism; the drift-guard check-8 asserts the CARD render (admin has ₪, staff doesn't).
- **M6 — per-target token invariant.** Each emitted copy's `?v=sh-<8>` == sha256-first8 of ITS LF git-blob (admin `sh-0bcd498e`, user `sh-2799ffb8` — different by design; note the financial literals are PRESENT-but-gated in both copies, not stripped — the flag selects at runtime, mirroring logger). `.gitattributes` pins eol=lf. All referencing pages retokenized, no cross-stamp.
- **M7 — drift-guard green + confidentiality asserted.** `node shared-web/emit.js --check` exits 0 (fails on drift, restored). check-8: user CARD render has no ₪/פיקס/שעות עבודה/רשומות/price; admin does. check-5/6 auto-cover injection + fail-secure default. 157 tests. (Test-comment corrected: the H2 assertion exercises the renderer's LOCAL function — admin's live path; the user-app shadows it via core-utils.)
- **M8 — exec-log maintained.** `docs/HEALTH-MAP` exec-log gains the PR-SHARE-6 row (+ flips PR-SHARE-4 #490 to ✅). Additive.

## PRODUCT-GRADE GATES
- **G1** N/A — card display; no customer error path.
- **G2 (rollback):** single-commit `git revert` restores the 2 hand copies + tokens + removes the canonical. Trivial, no data/CF.
- **G3** N/A — render-only, no data mutation.
- **G4** — the drift-guard (157: per-target byte-parity + token + check-5/6 + check-8 confidentiality/behavior) IS the customer-scenario proof.
- **G5** — Hebrew customer-facing strings preserved (H3 title/subtitle, badges) — unchanged text, only which field fills them.
- **G6** — declared behavioral changes: H1 (user escaping upgraded — safer), H3 (admin card title now the real service name). No data/API/route contract change; tokens = one-time re-fetch. Not breaking (both improvements, no consumer keys off them).
- **G7 (security):** PASS — H4 fail-secure confidentiality (default-deny + mechanical staff-no-₪ render guard), H1 escaping upgrade for user. Verified no leak introduced; the pre-existing world-readable `services[].fixedPrice` (unrendered to staff) is a tracked G7 data-access item, unchanged here.

## Anti-premature-closure
- devils-advocate = GO-WITH-CHANGES; its 2 corrections FOLDED IN (M3 H2-inert framing, M5 H4-preserves-not-achieves framing) + the test comment corrected. Confidentiality independently verified safe (probe: employees see no ₪ amount today; this PR preserves that).
- **2 follow-up chips spawned:** (a) `cases.js` reads an empty doc-level `fixedPrice` while the real value is at `services[].fixedPrice` (dead-field render — cosmetic/cleanup, not a leak); (b) duplicate `window.calculateRemainingHours` (renderer vs `calculators.js`) — latent SSOT divergence.
- Remaining גל-3ג: PR-SHARE-7/8 client-case-selector (189-line diff — own read-only investigation first). Then גל-3ד docs + 3ה TS.
