# Rubric — PR-U0: characterize the current admin client-modal behavior (modal unification)

**Scope:** First PR of the master-detail modal-unification track (`docs/PLAN-ADMIN-MODAL-UNIFICATION-2026-07.md` + `docs/WORK-PLAN-MODAL-UNIFICATION.md`). **Test-only, additive — ZERO production-code change.** Locks the plan/work-plan (docs, committed) + pins the CURRENT behavior of the two admin client modals AS-IS so the unified renderer (U4/U5) can prove it fixes the bugs and U7 can prove the buggy path is gone. **32 tests, 2 files, all green** (root vitest, happy-dom).

**Files (added):**
- `tests/unit/admin-panel/modal-unification-report-current-behavior.test.ts` (7) — D1/D2 **behavioral** (real `populateServiceCards` render) + source contracts.
- `tests/unit/admin-panel/modal-unification-management-contracts.test.ts` (25) — ManagementModal DOM contracts + the 16-callable §14 parity surface + write-path contracts.
- `docs/PLAN-ADMIN-MODAL-UNIFICATION-2026-07.md` + `docs/WORK-PLAN-MODAL-UNIFICATION.md` (the approved plan, reference).

## MUST
- **M1 — pins CURRENT behavior + green (characterization).** Asserts what the code DOES today, bugs included:
  - **D2 behavioral:** a 2-legal-procedure client (each with an active `stage_a`+`stage_b`) → `populateServiceCards` renders **2** cards, **all** carrying the LAST service's id (`srv_hagana`); `srv_tviaa` ("תביעה") is absent. + source pin `servicesMap.set(stage.id,` (`ClientReportModal.js:383`).
  - **D1 behavioral:** a mixed client (top-level `procedureType:'hours'`) with a ledger entry under the legal service's NAME → an **extra nameless total-0 phantom** card appears (2 cards vs 1 without the entry). + source pin of the `:505-531` fallback.
  - **32/32 pass**; a mis-captured assertion self-fails against current code.
- **M2 — no production-code change.** Adds ONLY the 2 `.test.ts` + this rubric (+ the plan docs). `ClientReportModal.js` / `ClientManagementModal.js` / `ServiceOverdraftResolution.js` are byte-unchanged.
- **M3 — the injector DOM contracts + §14 parity are pinned** (the silent-break class the unification most risks):
  - `.management-service-card[data-service-id]` (ServiceOverdraftResolution) + `.management-stage-name` textContent === `stage.name` (AddPackageToStage) + `.management-stage-info` + the 5 `data-service-action` buttons.
  - all **16** client-mutating callables present (incl. `updatePackagePurchaseDate` = Haim's named "change purchase date", #13) + `updateTimesheetEntry` payload (editHistory + autoGenerated) + `generateAndEmail` only-from-explicit-button.
  - the U1 target `hoursUsed = totalHours - hoursRemaining` + `isBlocked`/`isCritical` applied-from-CF-not-derived.
- **M4 — harness fidelity.** Mirrors `tests/unit/admin-panel/overdraft-debt-reframe.test.ts`: stub the window globals (`escapeHtml`, `ClientsDataManager.getClientTimesheetEntries`) BEFORE importing the IIFE, drive the REAL exported `window.ClientReportModal` instance against a happy-dom `#reportServiceCards` container. The source-contract file uses `readFileSync` (the sanctioned §15 VAL-1 pattern for the 16-callable pins). No production logic mocked.
- **M5 — characterization honesty.** Every assertion is the observed value; the bug pins are labelled `current bug` with the exact mechanism + line, and cross-referenced to the U-PR that removes/replaces them.

## PRODUCT-GRADE GATES
- **G1 (errors):** N/A — test-only, no customer path.
- **G2 (rollback):** `git revert <sha>` / delete the two files. Trivial.
- **G3 (monitoring):** N/A — read-only characterization; adds no write path.
- **G4 (test proves scenario):** PASS — the D1/D2 tests exercise the REAL `populateServiceCards` end-to-end (client fixture → rendered `.report-service-card` DOM), reproducing exactly the קובי/חליבה customer scenarios.
- **G5 (Hebrew UI):** N/A — no customer-facing strings added.
- **G6 (breaking change):** N/A — additive test file.
- **G7 (security):** N/A to change, RELEVANT to coverage — pins the injector DOM contracts + the write-path/external-action affordances the unification must not break (SEC-1/§15 territory).

## Anti-premature-closure
- **devils-advocate declared-skip (justified):** test-only, additive, zero production-code change → not a §3.8.4 trigger. (devils-advocate IS mandatory on the LATER U4/U5/U7, per the plan.) Run-green (32/32) + grader source-verify are the gates here.
- **Plan alignment:** this is PR-U0 of the 8-PR work-plan. **NEXT = U1** (Management reads stored `hoursUsed`, ~10 lines) — the pinned `total − remaining` derivation flips, and this suite's M3 hoursUsed test is updated as a declared behavioral change.
